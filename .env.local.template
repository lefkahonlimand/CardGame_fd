# 🔐 LOKALE SICHERE KONFIGURATION - TEMPLATE
# 
# ANLEITUNG:
# 1. Kopieren Sie diese Datei zu: .env.local.secure
# 2. Füllen Sie die Werte mit Ihren echten Daten aus
# 3. Die .env.local.secure Datei wird NICHT in Git gespeichert!

# SSH Konfiguration für Jetson Nano
JETSON_HOST=your-jetson-hostname
JETSON_IP=192.168.x.x
JETSON_USER=your-username
SUDO_PASSWORD=your-sudo-password

# Projekt-Pfade
PROJECT_PATH=/path/to/your/project

# Ngrok Konfiguration (optional)
NGROK_TOKEN=your-ngrok-token-here
CURRENT_NGROK_URL=https://your-tunnel.ngrok-free.app

# Quick Commands für Copy-Paste (Beispiele)
# SSH_CONNECT=ssh ${JETSON_HOST}
# SSH_STATUS=ssh ${JETSON_HOST} "cd ${PROJECT_PATH} && ./jetson-control.sh status"
# SUDO_RESTART=echo '${SUDO_PASSWORD}' | sudo -S systemctl restart card-game ngrok

# 🛡️ SICHERHEITSHINWEIS:
# - Verwenden Sie starke, einzigartige Passwörter
# - Teilen Sie diese Datei niemals
# - Erstellen Sie regelmäßige Backups (sicher verschlüsselt)
# - Bei Kompromittierung: Alle Passwörter sofort ändern
